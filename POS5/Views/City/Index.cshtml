@model IEnumerable<POS5.Models.City>
@{
    ViewBag.Title = "Index";
    //    Layout = "~/Views/_ViewStart.cshtml";
}

<h4>City List</h4>
<button type="submit" class="btn btn-info shiny" onclick="window.location='@Url.Action("New", "City")'"><i class="btn-label glyphicon glyphicon-plus"></i>New</button>


<table>
    <thead>
        <tr>
            <td>ID</td>
            <td>Name</td>
            

        </tr>
    </thead>
    <tbody>


        @if (!Model.Any())
        {
        }
        else
        {
            foreach (var lst_City in Model)
            {
                <tr>
                    <td>@lst_City.id</td>
                    <td>@lst_City.name</td>
                   





                    <td>
                        <a href="@Url.Action("Edit", new { id = lst_City.id })" class="btn btn-warning shiny input-xs"><i class="btn-label glyphicon glyphicon-edit">EDIT</i></a>
                        <a href="@Url.Action("Delete", new { id = lst_City.id })" class="btn btn-warning shiny input-xs"><i class="btn-label glyphicon glyphicon-edit">Delete</i></a>
                    </td>

                </tr>
            }
        }



    </tbody>


</table>





